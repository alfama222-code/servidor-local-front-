# export interface usuarioType {
#    id: string,
#    nome: string,
#     numero_identificacao: string,
#     data_nascimento: string,
#     email: string,
#    password: string,
#     telefone: string,
#     pais: string,
#    localidade: string,
#     enabled: boolean,
#    created_at: string,
#    update_at: string

components:
  schemas:
    users:
      type: object
      properties:
        id:
          type: intenger
          example: 1
        nome:
          type: string
          example: "servico de limpeza"
        numero_identificacao:
          type: string
          example: "123456789"
        data_nascimento:
          type: string
          example: "2022-01-01"
        email:
          type: string
          example: "ismaralfama222@gmail.com"
        password:
          type: string
          example: "123456"
        telefone:
          type: string
          example: "123456789"
        pais:
          type: string
          example: "brasil"
        localidade:
          type: string
          example: "sao paulo"
        enabled:
          type: boolean
          example: true
        created_at:
          type: string
          example: "2022-01-01"
        updated_at: 
          type: string
          example: "2022-01-01"
    #schemas para criar um novo usuario
    Createusers:
      type: object
      properties:
        nome:
          type: string
          examples: "servico de limpeza"
        numero_identificacao:
          type: string
          example: "123456789"
        data_nascimento:
          type: string
          example: "2022-01-01"
        email:
          type: string
          example: "ismaralfama222@gmail.com"
        password:
          type: string
          example: "123456"
        telefone:
          type: string
          example: "123456789"
        pais:
          type: string
          example: "brasil"
        localidade:
          type: string
          example: "sao paulo"
        enabled:
          type: bollean
          example: true

    loginUsers:
      type: object
      properties:
        email:
          type: string
          example: "ismaralfama222@gmail.com"
        password:
          type: string
          example: "123456"

      #schemas genericos para sucesso
    ResponseSucess:
      type: object
      properities:
      message:
        type: srtring
        example: "Servico criado comm sucesso"
      status:
        type: bollean
        example: true
      date:
        type: object
        $ref: "#components/schemas/prestacao"
  
    #schemas generico de resposta de erro
    ReferencesError:
      type: object
      properties:
        message: 
          type: string
          example: error ao buscar prestacao 
        status:
          type: bollean
          example: false
        data:
          type: object
          properties:
    